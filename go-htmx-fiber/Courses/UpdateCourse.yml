info:
  name: UpdateCourse
  type: http
  seq: 1

http:
  method: PUT
  url: "{{host}}/api/courses/3"
  body:
    type: json
    data: |-
      {
        "name": "Update course 4"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
