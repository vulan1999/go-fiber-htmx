info:
  name: Get Movies List
  type: http

http:
  method: GET
  url: http://localhost:3000/api/movies
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
