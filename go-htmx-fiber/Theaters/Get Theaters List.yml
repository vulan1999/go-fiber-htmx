info:
  name: Get Theaters List
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/theaters"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
